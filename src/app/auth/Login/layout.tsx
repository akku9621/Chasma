"use client";

import React from "react";

// Minimal layout for Login page, overrides root layout
export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
